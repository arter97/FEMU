#!/bin/bash

../configure \
    --extra-cflags="-O2 \
    -flto=auto \
    -ffat-lto-objects" \
    --extra-ldflags="-Wl,-Bsymbolic-functions \
    -flto=auto \
    -ffat-lto-objects \
    -Wl,-z,relro \
    -Wl,--as-needed" \
    --prefix=/usr \
    --sysconfdir=/etc \
    --libdir=/usr/lib/x86_64-linux-gnu \
    --libexecdir=/usr/lib/qemu \
    --firmwarepath=/usr/share/qemu:/usr/share/seabios:/usr/lib/ipxe/qemu \
    --localstatedir=/var \
    --disable-install-blobs \
    --interp-prefix=/etc/qemu-binfmt/%M \
    --localstatedir=/var \
    --disable-user \
    --enable-system \
    --enable-linux-user \
    --disable-xen \
    --enable-modules \
    --enable-module-upgrades \
    --enable-linux-aio \
    --audio-drv-list=pa,alsa,jack,oss,sdl \
    --enable-attr \
    --enable-bpf \
    --enable-brlapi \
    --enable-virtfs \
    --enable-cap-ng \
    --enable-curl \
    --enable-fdt \
    --enable-fuse \
    --enable-gnutls \
    --enable-gtk \
    --enable-vte \
    --enable-curses \
    --enable-virglrenderer \
    --enable-opengl \
    --enable-vnc-sasl \
    --enable-sdl \
    --enable-spice \
    --enable-linux-io-uring \
    --enable-libusb \
    --enable-usb-redir \
    --enable-libssh \
    --enable-zstd \
    --enable-nettle \
    --enable-libudev \
    --enable-vnc \
    --enable-vnc-jpeg \
    --enable-libpmem \
    --enable-kvm \
    --enable-vhost-net \
    --enable-lto \
    --enable-strip
